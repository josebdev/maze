import React from 'react'
import styled from 'styled-components'

const HeaderContainer = styled.header`
  display: flex;
`
const Header = () => {
  return <HeaderContainer>Logo - counter</HeaderContainer>
}

export default Header
